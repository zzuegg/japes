[package]
name = "bevy-benchmark"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy_ecs = "0.15"
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "ecs_benchmark"
harness = false

[profile.release]
lto = true
codegen-units = 1
